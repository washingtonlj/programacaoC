#include <stdio.h>
int main(){

int numeros[5] = {10, 20, 30, 40, 50}; //Inicialização do array com valores inteiros

    // Acessando e imprimindo elementos específicos do array
    printf("O primeiro numero é: %d\n", numeros[0]);
    printf("O segundo número é: %d\n", numeros[1]);
    printf("O quarto número é: %d\n", numeros[3]);

    return 0;
}